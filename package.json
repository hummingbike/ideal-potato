{
  "name": "ideal-potato",
  "private": true,
  "version": "0.0.0",
  "description": "나혼자만 인테리어 — grid-based interior layout planner",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
